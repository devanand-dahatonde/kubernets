apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: ds
spec:
  selector:
    matchLabels:
      env: dev
  template:
    metadata:
       name: tmp-game
       labels:
         env: dev
    spec:
       containers:
        - name: c1
          image: nginx
          ports:
          - containerPort: 80
